NODE_ENV=development

# Registry URL for agent registration and discovery (default: https://automata.apptrixcloud.com for production and https://automata-dev.apptrixcloud.com for development)
REGISTRY_URL=

# LLM settings (supports openai/claude/gemini/deepseek/openrouter)
# Optional; only needed if you use callLLM helper
LLM_PROVIDER=openai
LLM_MODEL=gpt-4o-mini
LLM_API_KEY=
LLM_TEMPERATURE=0.7

# Client identifier for auth/token (stable across runs)
JWT_SECRET=change-me-in-prod-32chars-min

# Server settings (bind address/port). HOST is only for bind, not for public URL.
HOST=0.0.0.0
PORT=4000

# Public endpoint (required in production, HTTPS). Example: https://provider.apptrixcloud.com
PUBLIC_ENDPOINT=
